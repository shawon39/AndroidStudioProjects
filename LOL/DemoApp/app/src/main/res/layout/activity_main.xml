<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="15dp"
    tools:context="com.shawon.demoapp.MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Greetings appear here.."
        android:textSize="20dp"
        android:textColor="#009385"
        android:id="@+id/grettings_id"
        android:layout_marginTop="21dp"
        android:layout_marginBottom="10dp"/>

    <Button
        android:id="@+id/button"
        android:layout_width="350dp"
        android:layout_height="55dp"
        android:layout_below="@+id/grettings_id"
        android:onClick="showGreattings"
        android:text="Open Second Activity" />

    <Button
        android:layout_width="350dp"
        android:layout_height="55dp"
        android:text="Open Third Activity"
        android:onClick="showGreattings"
        android:layout_below="@+id/button"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="15dp" />

</RelativeLayout>
